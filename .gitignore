# === Vendored harness — its OWN git repo, an 800M venv, and the runtime API key live here ===
computer-use-preview/

# === Secrets (never commit) ===
.env
*.env
.env.*
*.key
secrets*.json

# === Python ===
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# === Local tooling / session state ===
.claude/

# === Bulky/regenerable probe artifacts (keep .py and .json) ===
probe/*.png
probe/*.jpg
probe/*.jpeg
probe/*.log
# KEEP: provenance of the genuine Gemini 3.5 Flash inverter solve (prize evidence — referee PASS, model line)
!probe/gate2_3.5_capture.log

# === Local library store (regenerable embeddings cache; Atlas is the real backend) ===
operator/skills/library_local.json

# === Runtime demo log (regenerable; the dashboard referee lamp reads it) ===
operator/run.log

# === OS ===
.DS_Store
